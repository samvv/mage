
from magelang.ast import Grammar
from magelang.lang.rust.cst import RustSyntax
from magelang.util import todo


def generate_visitor(
    grammar: Grammar,
    prefix: str = '',
    debug: bool = False
) -> RustSyntax:
    todo()
